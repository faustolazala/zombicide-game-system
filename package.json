{
  "name": "zombicide-game-system",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Foundry VTT 13.351 game system foundation for Zombicide.",
  "scripts": {
    "test": "node --test --test-isolation=none",
    "check": "node --check zombicide.mjs && node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=20"
  }
}
